{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "mkdir -p storage/framework/sessions storage/framework/cache storage/framework/views && chmod -R 775 storage && export SESSION_DRIVER=file ASSET_URL=https://godong-ijo-laravel-production.up.railway.app && php artisan db:seed --class='Database\\Seeders\\AdminSeeder' --force || true; exec php -S 0.0.0.0:$PORT -t public",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
